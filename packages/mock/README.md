# packages/mock

> 本包用于存放项目的 Mock 数据与本地假接口，便于前端在无真实后端时进行开发与联调。

维护说明：放置可复用的测试数据与接口实现；若数据结构变更，请同步更新 `core` 中的类型定义。

运行（示例）：

```bash
# 在根目录安装依赖后，可单独运行 mock 服务（若已提供脚本）
pnpm --filter @chisha/mock dev
```
